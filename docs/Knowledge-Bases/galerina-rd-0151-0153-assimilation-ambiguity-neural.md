# RD-0151..0153 — Dynamic Assimilation · Tri-Logic Ambiguity · Governed AI (notes 13-15)

**Source.** Owner notes `notes/75-improvments-r-d-13..15.md` (2026-06-27) — the tail of the "automate the
defence" series. Same three-layer pattern as [[galerina-rd-0138-0143-photonic-security-automation]] and
[[galerina-rd-0144-0149-governed-chaos-multisubstrate]]. (RD-0150 is reserved in the worker session.)

**Hub disposition.** Numbered + ZT-scored + proven/disproven. Proof:
`scripts/rd-0151-0153-assimilation-ambiguity-neural-proof.mjs` — **6/6 V GREEN, 4 excluded.** Deep per-claim
matrix to the encryption R&D worker.

## Per-note verdicts

| RD | Note | Topic | ZT | Verdict |
|---|---|---|---|---|
| 0151 | 13 | **Dynamic Assimilation** (hot-plug HW — the "missing GOVERNED-CHAOS pillar") | **6** | "Hardware Citizen" onboarding — attest a hot-plugged accelerator/FPGA/quantum/neuromorphic device before granting capabilities → ✅ **genuine net-new seam** but re-derives the shipped bridge-attestation + capability charter, **HW-gated** (X1). Tri-Pipe "instruction transmutation 0-overhead" ❌ (recompile cost) + Photonic "spectral discovery maps the new chip at 0 clock cycles" ❌ (V2). The attest-before-trust direction is strongly zero-trust-aligned. |
| 0152 | 14 | **Tri-Logic Ambiguity** ("treat 0 as an active defensive asset") | **5** | K3-0-as-asset re-derives the substrate-noise model + `partialReturn`. **"Stochastic Mask = feed the attacker dummy data and keep the LIVE flow alive" ❌** = fail-open unless a scoped quarantine DECOY (the shipped Lane-−1 Decoy Substrate; X4). "Superposed execution = parallel threads for ALL outcomes" ❌ (exponential; bounded form = NMR, X3). Photonic phase-cancellation "0-cycle erase" ❌ (V1+V2). |
| 0153 | 15 | **Governed AI in AI/ML/Neuro Nets** (governance in the tensor path) | **6** | **Immutable Weight Charters + anti-poisoning invariant** ✅ — a weight update can't lift a charter-denied capability (V1 No-Coercion); re-derives AI-governance + attestation + capability-strip; genuinely valuable. Tri-Pipe "activation-vector clamping `P_safe(H)`" = a real ML technique but runs INSIDE the net → **out of the governance core** (ext-bridge, X2); "clamp-and-continue" carries the declassifier asterisk. Photonic "phase-barrier IS the alignment gate, 0-cycle" ❌ (V1: optical control-λ can't be the gate). |

## What's proved (V1–V3)
- **V1 No-Coercion** — an optical/analog control-wavelength can't be the gate (RD-0151/0152/0153 photonic claims),
  and a poisoned weight update can't lift a charter-denied capability to ALLOW (RD-0153 anti-poisoning).
- **V2 0-cycle REFUTE** — instruction transmutation (0151), phase-cancellation (0152), phase-barrier (0153) all
  claim 0 clock cycles; applying any transform to n operands is Θ(n).
- **V3 "0 as a defensive asset" / "clamp-and-continue" is sound only FAIL-CLOSED** — `authorize(0)=false`; the
  shipped `maskByVerdict(0)` returns a Masked sentinel (no real **or fabricated** value flows). RD-0152's
  "feed dummy data into the live flow" diverges from this and is fail-open unless a scoped decoy.

## Net-new genuine seams (owner-gated, design-only)
1. **Hardware Citizen attestation/onboarding** (RD-0151) — attest a hot-plugged substrate before it gets a
   capability charter. Re-derives bridge-attestation, extended to dynamic HW; HW-gated (DRCM Ph5 / #102-106).
2. **Model Weight Charter + anti-poisoning invariant** (RD-0153) — a signed charter over a model's weights so a
   capability-adding weight drift is denied (No-Coercion at the tensor admission boundary). The sound, in-core
   half of "governed AI"; the activation-clamping half stays in the ext-bridge.

Both reinforce the cross-cutting RD-0138-0149 finding: every "Lane 0 = continue" (clamp / mask / dummy-data /
superpose) is sound ONLY as a fail-closed declassifier, scoped decoy, or availability-degrade — never a silent
safety bypass. Crypto stays Binary; photonic = degrade-only under the digital gate.
